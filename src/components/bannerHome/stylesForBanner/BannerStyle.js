import styled from "styled-components";
import logo from "../../../asserts/images/shadow.png"

export default styled.div`
    margin-top:0;
    height: 866px;
    position: relative;
    background: url(${logo});
`;